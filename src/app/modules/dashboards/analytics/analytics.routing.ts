import { Route } from '@angular/router';

import { AnalyticsComponent } from './analytics.component';

export const  projectAnalyticsRoutes: Route[] = [
    {
        path     : '',
        component: AnalyticsComponent
    }
];